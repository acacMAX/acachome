name = "acac-personal-homepage"
type = "javascript"
compatibility_date = "2024-01-01"

# Workers Sites 配置 - 直接托管静态文件
[site]
bucket = "./"

# 部署配置
[vars]
ENVIRONMENT = "production"

# 路由配置（部署后需要设置）
# routes = [
#   { pattern = "yourdomain.com/*", zone_name = "yourdomain.com" }
# ]

# 自定义域名（可选）
# routes = ["acachome.yourdomain.com/*"]